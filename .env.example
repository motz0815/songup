# SongUp monorepo — environment variables per Vercel zone (project).
# Copy the vars a zone needs into that Vercel project's Environment Variables,
# or into apps/<zone>/.env.local for local development.
#
# ─────────────────────────────────────────────────────────────────────────────
# apps/web  (PRIMARY zone — owns songup.tv)
# ─────────────────────────────────────────────────────────────────────────────
# Multi-zone rewrite targets (internal deployment URLs of the other zones).
# In local dev these default to localhost:3001/3002/3003 and can be omitted.
APP_DOMAIN=https://app.songup.tv
DOCS_DOMAIN=https://docs.songup.tv
BLOG_DOMAIN=https://blog.songup.tv
# Public client config (the landing page reads public Convex stats + PostHog).
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
NEXT_PUBLIC_POSTHOG_KEY=phc_xxx
NEXT_PUBLIC_SITE_URL=https://songup.tv

# ─────────────────────────────────────────────────────────────────────────────
# apps/app  (SECONDARY zone — /host /room /pay /discord /api /flask)
# This is the ONLY project that deploys Convex (see vercel-build).
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
NEXT_PUBLIC_POSTHOG_KEY=phc_xxx
NEXT_PUBLIC_STRIPE_ROOM_PRICE=price_xxx
NEXT_PUBLIC_SITE_URL=https://songup.tv
# Required for `convex deploy` during the production build:
CONVEX_DEPLOY_KEY=prod:your-deploy-key
# Note: Stripe secret keys, PostHog server keys, and auth secrets live in the
# Convex deployment's environment (Convex dashboard), NOT in Vercel.

# ─────────────────────────────────────────────────────────────────────────────
# apps/blog  (SECONDARY zone — /blog)  — optional
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=https://songup.tv

# ─────────────────────────────────────────────────────────────────────────────
# apps/docs  (SECONDARY zone — /docs)  — no environment variables required
# ─────────────────────────────────────────────────────────────────────────────
