"use client"

import { Room } from "@/types/global"

export default function Host({ room }: { room: Room }) {
    return (
        <div>
            This is a client component that will render the host interface
        </div>
    )
}
