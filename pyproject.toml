[project]
name = "democratune-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.121.2",
    "python-dotenv==1.0.0",
    "ytmusicapi==1.11.1",
]

[tool.vercel]
entrypoint = "api.index:app"
